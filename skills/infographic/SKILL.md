# Infographic

Transform scoped evidence into a visual hierarchy before rendering. Prefer
deterministic geometry and editable SVG/scene IR over free-form image output.
Use `visual.flowchart` for process structures, then run evidence, graph,
contrast, density, and legibility checks at the intended display size.
