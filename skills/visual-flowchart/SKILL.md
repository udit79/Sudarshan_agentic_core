# Visual flowchart

Return a renderer-neutral graph IR with nodes, edges, groups, evidence IDs,
layout hints, and an accessibility summary. Validate reachability, required
labels, duplicate IDs, edge references, and layout bounds before producing an
SVG preview. A parent skill owns the slide or document; this skill owns only
the diagram artifact and its quality report.
