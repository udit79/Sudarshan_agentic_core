# Sudarshan 2.0 assumptions and deployment checklist

This file is a living risk register for assumptions made while implementing
the execution plan. Each assumption must be confirmed, changed, or retired
before production deployment. Coding agents should add a new ID instead of
silently changing a public contract.

| ID | Assumption today | Evidence / scope | Deployment action | Status |
|---|---|---|---|---|
| AS-01 | SQLite queue, DAG store, progress store, and LangGraph checkpoints are single-instance local stores. | Current implementation uses local SQLite paths under `artifacts/.state/`. | Replace or front them with a shared broker/database and define ownership, backup, failover, and migration procedures before multi-host workers. | Open |
| AS-02 | Python threads cannot be forcibly stopped. | Scheduler and child runtime use cooperative events, network timeouts, and killable subprocess boundaries. | Require every provider adapter to observe cancellation and bound network/process work; add provider-specific watchdog tests. | Open |
| AS-03 | Skill discovery can contain skills that are not executable in the current deployment. | `visual.flowchart` is catalogued but currently reports `available=false`. | Gate UI/Harness selection on availability and register the executable adapter before enabling the skill. | Open |
| AS-04 | The scheduler stores only safe typed skill references, not model output or raw memory. | `skill_result_json` is limited to `SkillResult`-style references. | Verify retention, access control, artifact lineage, and recovery behavior for result references. | Open |
| AS-05 | MCP schema compatibility tests are local; a live external Harness/MCP client has not yet completed the full lifecycle. | T12 local adapter and schema tests pass. | Run a staging test with the actual Harness client, reconnect, cancellation, artifact download, and an independent MCP client. | Open |
| AS-06 | Provider usage may be unavailable or estimated for some model/media adapters. | `UsageRecord.is_estimate` exists and gateway counts are not billing truth. | Require provider-normalized usage where available and label estimates in operator dashboards and promotion gates. | Open |
| AS-07 | Runtime secrets are supplied through deployment configuration, not committed files. | `.env` is ignored and has not been edited by this work. | Use a secret manager, rotation policy, least-privilege service accounts, and secret-scan gates. | Open |
| AS-08 | Classification and distribution policy values are valid at every boundary. | `AdvisoryRequest` and NTRO policy validators enforce the current values. | Confirm the production classification taxonomy, cross-case rules, retention, and external-agent restrictions with the security owner. | Open |
| AS-09 | Local artifact paths are available to the current process. | Artifact manifests expose stable URIs while local development uses filesystem storage. | Use durable object storage, immutable versioning, checksum verification, signed access, and disaster recovery before distributed deployment. | Open |
| AS-10 | In-process child concurrency counters are sufficient for one runtime instance. | `SkillRuntime` limits active children per parent in memory. | Move concurrency reservations to the shared budget/queue control plane for multi-process execution. | Open |
| AS-11 | A successful quality verdict requires deterministic validators in addition to model critics. | The plan requires schema, evidence, render, media, and visual gates. | Define release thresholds and matched legacy-versus-staged evaluations for every artifact class. | Open |
| AS-12 | The current skill catalog version is `1.0.0` for the first canonical adapters. | Catalog manifests define explicit versions and legacy aliases. | Freeze compatibility policy, migration rules, deprecation dates, and signed manifest distribution. | Open |
| AS-13 | The first budget ledger is process-local and child-skill scoped. | `BudgetController` is wired into `SkillRuntime` and exposes safe counters through MCP. | Persist reservations/usage and coordinate them across workers before allowing multi-process child execution or treating counters as billing truth. | Open |
| AS-14 | The first artifact cache is process-local SQLite and exact-match only; cache hits are permitted only after an explicit `quality_status=passed` result. | `CacheStore` fingerprints skill/version, contract, input refs, tool/provider versions, model policy, and authorization scope; it stores artifact references and quality metadata, not raw prompts or memory. | Move cache coordination to shared durable storage for multi-host deployment, define tenant/case namespaces and invalidation events, and measure false-hit/stale-hit rates before enabling semantic reuse. | Open |
| AS-15 | Packaged skill resources use a validated folder contract and JSON manifests; full instruction bodies/resources are loaded only after selection. | `skills/` contains versioned manifests, `SKILL.md`, schemas, policies, failure cases, and smoke eval fixtures; the Harness catalog consumes these manifests with legacy fallback. | Add signed package distribution, owner/version compatibility rules, schema migration policy, resource integrity checks, and a reviewed install path for user-created skills before production enablement. | Open |
| AS-16 | The first presentation IR is renderer-neutral but is not yet the renderer or visual QA source of truth. | `pipelines/ppt/schemas.py` now validates deck plans, slide content, visuals, evidence bindings, normalized bounds, and targeted repair patches while preserving `PresentationOutput`. | Connect IR conversion to the PPT/SVG renderers, add slide image rendering and visual regression gates, and define migration/round-trip rules before switching production deck generation. | Open |
| AS-17 | The staged PPT flow is opt-in and still converts its typed plan into the legacy `PresentationOutput` delivery contract. | `SUDARSHAN_PPT_FLOW=staged` adds grounding, deck planning, visual routing, slide content, and quality tasks; legacy mode remains the default and both paths use CrewAI callbacks. | Run matched legacy/staged evaluations, connect `DeckPlan` to real child-skill execution and renderers, then promote the staged flag only after quality, cost, and latency thresholds pass. | Open |
| AS-18 | The first flowchart renderer uses deterministic in-process geometry and `python-pptx`; it does not yet provide arrowhead styling, automatic text measurement, or rendered-image QA. | `VisualIR` converts to strict `FlowchartSpec`, which feeds one layout into SVG and editable PPTX output; graph cycles, unknown edges, isolated nodes, and off-canvas boxes fail before rendering. | Add font-aware measurement, routed connectors/arrowheads, slide image rendering, visual regression, and renderer versioning before production promotion. | Open |
| AS-19 | The first T19 quality layer treats normalized geometry as the deterministic visual proxy; it does not claim that a slide image is visually acceptable at projector scale. | `inspect_flowchart` reports overlap, edge crossing, off-canvas, and label overflow diagnostics, with evidence source maps and bounded `RepairPatch` outputs. | Add actual slide rasterization, font-aware text measurement, contrast/density checks, visual snapshots, and human preference thresholds before release. | Open |
| AS-20 | The first T20 presentation vertical slice keeps DAG/service state in the local process while durable node state and artifact manifests live in SQLite/filesystem paths. | `PresentationVerticalSlice` runs `visual.flowchart → visual.flowchart.qa → presentation.flowchart.delivery`, registers only quality-gated artifact manifests, and projects safe quality/artifact data. | Persist or reconstruct vertical-slice context after worker restart, connect it to the application/MCP boundary, and use shared queue/object storage before multi-host production. | Open |
| AS-21 | Video scene manifests are durable local JSON and scene reuse is exact-fingerprint only; composition remains a final local FFmpeg step. | Native video now persists scene status/fingerprint/paths, renders missing scenes with bounded workers, reuses matching files, preserves storyboard order, and includes authorization scope in fingerprints. | Move manifests/leases to the shared control plane, add provider-normalized media usage, harden crash recovery and cleanup, and add rendered-media QA before distributed production. | Open |
| AS-22 | AntV SSR may be slower than the request budget or keep a renderer process alive after a timeout; a successful file alone is not a quality verdict. | The real final-output fixture is rendered through a separate Node worker, with a bounded parent timeout, explicit `antv`/`fallback` mode, warning metadata, and deterministic SVG integrity checks. | Exercise SSR timeout and child-kill behavior in staging, add PNG/rasterized visual QA, retain fallback warnings in operator dashboards, and do not promote fallback-only output as equivalent to native AntV quality. | Open |
| AS-23 | Cognee can return useful graph/vector context, but retrieval ranking, stage budgets, provenance projection, and promotion policy must remain under Sudarshan control. | T23 adds query-aware ranking, duplicate removal, progressive stage profiles, typed `ContextPack` output, stable retrieval traces, and orchestrator propagation while keeping Cognee behind `MemoryManager`. | Add dataset freshness/version checks, contradiction/supersession lifecycle, retrieval-quality benchmarks, deletion/forgetting tests, and shared-dataset access controls before production memory promotion. | Open |
| AS-24 | Local lifecycle transitions can hide stale or retracted records, but they do not yet update every historical Cognee graph node across worker restarts. | T24 adds active/pending-review/superseded/retracted/expired states, explicit supersession, scope-checked retraction, and opt-in backend purge capability. | Persist lifecycle state in the shared control plane or Cognee metadata, reconcile it after restart, add authenticated dataset deletion tests, and require operator confirmation for irreversible purge. | Open |
| AS-25 | The LinkedIn parent skill can produce a typed visual-child call, but the current first slice does not yet execute that call or reconcile its returned artifact into the parent output. | T25 adds claim bindings, a deterministic humanizer report, draft-only approval state, and a bounded `SkillCall` builder for `visual.flowchart`; publishing remains outside the pipeline. | Register an executable visual adapter, invoke it through `SkillRuntime`/DAG, persist the `SkillResult`, and test artifact/quality-reference reconciliation before enabling the child in production. | Open |
| AS-26 | The first observability store is local SQLite and receives only safe projections from progress and child-runtime events; it is not yet a distributed trace or billing ledger. | T26 adds correlated run/task/skill identifiers, usage/cache/wait/quality/artifact counters, a dashboard-safe `RunSummary.telemetry`, and `/runs/{run_id}/telemetry`; prompts and raw memory are excluded. | Move telemetry to the shared control plane or OpenTelemetry-compatible collector, define retention/access policy, reconcile events across workers, and compare provider billing with estimated usage before production reporting. | Open |
| AS-27 | The first security slice enforces trust and data-boundary rules in-process; it is not yet a signed-package or OS/container sandbox. | T27 adds child identity propagation from trusted parent context, manifest trust-tier checks, explicit publish approval checks, prompt-injection markers for retrieved source content, recursive secret redaction, query hashing in audit start records, and credential filtering in cache metadata. | Add signed manifest/package verification, authenticated distributed authorization, OS/container sandbox enforcement, secret-scanner CI, and multi-worker security tests before production or untrusted skill installation. | Open |
| AS-28 | Local SQLite queues and DAG state can recover a stale job, but a production deployment still needs a shared lease/control plane and explicit crash-injection evidence. | T28 adds durable DAG context recovery for restarted bridges, progress replay from a cursor, renderer-failure assertions, and partial video-package retry coverage; existing scheduler tests cover timeout, duplicate admission, retry/dead-letter, cancellation, lease renewal, and stale-lease recovery. | Run kill-9/host-loss tests with multi-worker shared storage, verify idempotency under concurrent duplicate workers, and define artifact cleanup/reconciliation for abandoned leases before multi-host production. | Open |
| AS-29 | Harness product presentation can be delivered as replaceable web plugins without changing official Harness branding or backend contracts. | `client-ui-sudarshan` owns brand slots and `client-ui-sudarshan-theme` owns the stylesheet; the web patch loads both and the client typecheck/build pass locally. | Run the actual packaged Harness preview and a white-label replacement profile, then add browser E2E coverage for plugin load, HMR teardown, accessibility, and theme fallback. | Open |
| AS-30 | A previously committed Harness preview credential must be treated as compromised even after `.dsh-preview/` is deleted and ignored. | The generated preview directory was removed from the working tree and `.gitignore` now blocks it, but the old commit remains in repository history until history is rewritten. | Revoke/rotate the credential, scan all remotes and forks, rewrite history if it was pushed, and add secret-scanning CI before release. | Open |
| AS-31 | The current synchronous `ingest_file()` and plain `raw_text` contract can remain as a compatibility path while a typed asynchronous evidence pipeline is introduced. | T31 adds strict manifest/evidence/event/quality contracts; T33 adds `/ingestions`, durable local admission, status, cancellation, retries, and keeps `/ingest` synchronous. | Do not claim multimodal evidence readiness until T34–T38 pass. | Open |
| AS-32 | Original files and derived evidence need separate durable storage; Cognee is not a raw artifact store. | The current adapter writes one `KnowledgeUnit` directly after extraction and local artifacts are process/filesystem scoped; T32 now hashes and validates staged sources without persisting raw data in safe metadata. | Add immutable source storage, evidence retention/deletion policy, and evidence-ID lookup before distributed ingestion. | Open |
| AS-33 | Modality plugins can use deterministic parsers first and bounded OCR/VLM fallbacks without making the central Harness agent the parser. | T32 performs trust-boundary checks; T34 now preserves typed evidence for text, PDF, PPTX, image, and video, including page/slide/timestamp locations, confidence, scene parents, and parser/model provenance while retaining legacy outputs. | Add targeted fallback budgets, explicit partial/failure states, adaptive scene detection, derived artifacts, and evidence persistence in T36–T37. | Open |
| AS-34 | Page/slide/scene fan-out can share the existing DAG, scheduler, cache, and telemetry contracts once ingestion jobs have their own manifest and idempotency key. | T33 now uses a dedicated local durable ingestion scheduler with bounded workers, leases, retry/dead-letter, cooperative cancellation, source-hash idempotency, and safe status receipts. | Run T34–T38 with failure injection, duplicate workers, cancellation, cache invalidation, and cross-case authorization before production ingestion. | Open |
| AS-35 | Structure compilation must remain deterministic and source-mapped; model-based relationship inference cannot be the only way to build retrieval context. | T35 now recursively builds deterministic parent, temporal, page/slide sequence, and validated explicit-reference edges plus structure-aware chunks, preserving evidence IDs and source-map completeness; the exact chunks are not yet persisted or indexed. | Add shared evidence/chunk storage, semantic relationship enrichment behind explicit provenance, retrieval indexes, and cross-case authorization tests in T36–T38. | Open |
| AS-36 | Exact evidence must remain outside semantic memory, while skills need a scoped and modality-aware lookup boundary. | T36 adds a local SQLite evidence index for typed blocks, chunks, and relationships, plus bounded summary projection through `MemoryManager`; text, visual, table, video-segment, and exact-evidence methods enforce User/Case/Task and classification checks. | Replace local SQLite with shared durable/vector/visual indexes, add immutable object storage and retention, and benchmark retrieval and projection under concurrent multi-worker deployment. | Open |
| AS-37 | Ingestion stages must be budgeted and reusable without allowing a cache hit to cross a security scope. | T37 adds a local per-ingestion budget controller for parser/OCR/vision/summary/embedding work and fan-out, fingerprints and a scoped SQLite derived-stage cache, preflight charging for image/PDF/video provider work, explicit fallback markers with `PARTIAL` receipts, durable scheduler queue-wait telemetry, an estimate-versus-actual usage ledger, a bounded opt-in retry seam for transient vision-provider failures, and startup cleanup for expired cache metadata and abandoned claims. | Reconcile every provider response where usage is available, coordinate retry reservations across workers, share cache leases/object storage, define artifact retention/garbage collection, and clean abandoned files in production-like storage. | Open |
| AS-38 | A local benchmark can catch retrieval and traceability regressions before production promotion, but synthetic fixtures are not evidence of field quality. | `ingestion_pipelines.evaluation` compares flat-text and typed-evidence retrieval with reviewed evidence IDs and records coverage, recall, faithfulness, tokens, cost, latency, cache, queue, repair, and correction metrics. | Run the same report against sanitized scanned PDFs, table-heavy PDFs, PPTX, infographics, long videos, paraphrased queries, partial failures, and cross-skill reuse on shared staging infrastructure; archive the report and signed promotion decision. | Open |

## Update rule

At the end of each implementation ticket, update this file if a new
assumption, resolved risk, or deployment dependency was discovered. An
assumption is not considered resolved merely because a local unit test passes;
it is resolved only when the deployment action has evidence from staging or
production-like infrastructure.
