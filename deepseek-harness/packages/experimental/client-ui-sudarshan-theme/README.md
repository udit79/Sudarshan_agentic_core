# Sudarshan Harness theme plugin

This package owns the product presentation layer only: the visual token and
component overrides in `src/styles/sudarshan.css`. It is loaded after the
generic Harness theme and can be removed or replaced without changing the
Harness UI packages or the Sudarshan agent runtime.
